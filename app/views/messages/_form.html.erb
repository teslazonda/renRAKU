<%= simple_form_for [@student, @message] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :photo %>
  <%= f.submit 'Add message', class: 'btn btn-danger mt-2 btn-lg btn-block' %>
<% end %>
