const insertComment = () => {
const comments = document.querySelector("#comment<%= @comment.message.id %>-placeholder")
const partial = "<%= j render @comment %>"
comments.insertAdjacentHTML('beforebegin', partial )
  document.querySelectorAll('.text').forEach((input) => {
    input.value = ""
  })
}
insertComment()
